<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.intelab.joblab.presentation.ui.home.profile.viewmodels.ProfileAddJobReferenceViewModel" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="horizontal">

        <NumberPicker
            android:id="@+id/pickerYear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:value="@={viewModel.pickedEndedYear}"
            app:maxValue="@{viewModel.currentYear}"
            app:minValue="@{viewModel.minEndedYear}" />

        <NumberPicker
            android:id="@+id/pickerMonth"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:value="@={viewModel.pickedEndedMonth}"
            app:displayedValues="@{viewModel.displayEndedMonthValues}"
            app:maxValue="@{viewModel.maxEndedMonth}"
            app:minValue="@{viewModel.minEndedMonth}" />

    </LinearLayout>
</layout>