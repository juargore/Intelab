<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.intelab.joblab.presentation.ui.home.main.viewmodels.HomeViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:background="@color/gray_900">

        <ImageView
            android:id="@+id/iv_close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="12dp"
            android:onClick="@{() -> viewModel.onCloseMenuClicked()}"
            android:src="@drawable/ic_close_menu"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="ContentDescription" />

        <com.intelab.joblab.presentation.ui.views.CircularCompletionView
            android:id="@+id/cpv_avatar"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="16dp"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/iv_close"
            app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize50Percent"
            app:url_no_cache="@{@string/small_size_user_avatar(viewModel.url)}" />

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@{viewModel.userFullName}"
            android:textColor="@color/white"
            app:layout_constraintStart_toStartOf="@id/cpv_avatar"
            app:layout_constraintTop_toBottomOf="@id/cpv_avatar"
            tools:text="Mónica Robles Cardona" />

        <TextView
            android:id="@+id/tv_jobs"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="end"
            android:text="@{viewModel.preferableJobs}"
            android:textColor="@color/gray_400"
            android:textSize="11sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@id/cpv_avatar"
            app:layout_constraintTop_toBottomOf="@id/tv_name"
            tools:text="Cajero / Auxiliar Administrativo / Almacenista" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:background="@color/gray_500"
            app:layout_constraintTop_toBottomOf="@id/tv_jobs" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>